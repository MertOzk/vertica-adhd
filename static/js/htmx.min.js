// Placeholder. In production, replace this file with the real htmx.min.js
// from https://unpkg.com/htmx.org@2.0.3/dist/htmx.min.js
//
// Leaving a stub here so the dashboard loads cleanly during build/test.
// To fetch the real file at build time, add this to the Dockerfile:
//   RUN curl -sL https://unpkg.com/htmx.org@2.0.3/dist/htmx.min.js -o /app/static/js/htmx.min.js
//
// For now, polling won't happen; reload the page manually to refresh state.
console.info("htmx.min.js is a placeholder. See comment at top of file.");
